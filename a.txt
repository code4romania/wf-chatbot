for each attempt
    response= prompt the model
    if response parsable:
        break
    history: user: prompt, bot: response, user: make sure you retutn a valid python dictionary example: [a,b,c]
    response= prompt the model with history

    if response parsable: break
    
